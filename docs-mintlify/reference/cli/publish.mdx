---
title: "ralphy publish"
description: "Publish a formed unit to social platforms via Postiz (#501): binds accounts, uploads the unit's media, creates one post per target, and appends the results to t"
---

{/* Auto-generated — edit `cli/commands/<verb>.ts` instead. Regenerate via `bun run docs:cli`. */}

## Summary

```bash
ralphy publish [options] <project> <unit-slug>
```

**Common flags**

| Flag | Description |
|---|---|
| `--targets <list>` | Comma-separated targets (youtube \| tiktok \| instagram \| x) |
| `--at <iso>` | Schedule datetime (ISO). Omit to post immediately |
| `--account <map>` | Explicit account bindings, e.g. "youtube=&lt;integration-id&gt;,x=&lt;id&gt;" |

<Expandable title="Full reference">

**Flags**

| Flag | Description |
|---|---|
| `--targets <list>` | Comma-separated targets (youtube \| tiktok \| instagram \| x) |
| `--at <iso>` | Schedule datetime (ISO). Omit to post immediately |
| `--account <map>` | Explicit account bindings, e.g. "youtube=&lt;integration-id&gt;,x=&lt;id&gt;" |
| `--force <reason>` | Bypass the readiness gate with an explicit reason (logged to user-prompts.jsonl) |
| `-h, --help` | display help for command |

</Expandable>
