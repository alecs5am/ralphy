---
title: "ralphy queue"
description: "Manage the local job queue (add work, watch progress, cancel, retry)"
---

{/* Auto-generated — edit `cli/commands/<verb>.ts` instead. Regenerate via `bun run docs:cli`. */}

## Summary

```bash
ralphy queue [options] [command]
```

**Common flags**

| Flag | Description |
|---|---|
| `--auto-start` | Spawn the daemon if it's not running before applying the subcommand (default off) (default: false) |

<Expandable title="Full reference">

**Flags**

| Flag | Description |
|---|---|
| `--auto-start` | Spawn the daemon if it's not running before applying the subcommand (default off) (default: false) |
| `-h, --help` | display help for command |

</Expandable>
