---
title: "Ingestion nodes"
description: "Workflow-graph ingestion nodes: ports, params, executor status, and spend class."
---

{/* Auto-generated — edit `cli/lib/schemas/workflow.ts` + the executor registry instead. Regenerate via `bun run docs:nodes`. */}

D. Ingestion — graph sources, no in-ports; all emit normalized source-item[].

Facts on this page are introspected from `cli/lib/schemas/workflow.ts` and the live executor registry — never hand-edited.

### `web-scrape`

**Status:** executable · free

_No schema description yet._

**Ports**

| Direction | Port | Type |
|---|---|---|
| in | — | — |
| out | `out` | source-item[] |

**Params** — passthrough (typed once the executor lands; any key is accepted).

**Example**

```json
{
  "version": "2.0",
  "name": "web-scrape-example",
  "nodes": [
    {
      "id": "web-scrape",
      "type": "web-scrape"
    }
  ]
}
```

### `actor`

**Status:** executable · free

_No schema description yet._

**Ports**

| Direction | Port | Type |
|---|---|---|
| in | — | — |
| out | `out` | source-item[] |

**Params** — passthrough (typed once the executor lands; any key is accepted).

**Example**

```json
{
  "version": "2.0",
  "name": "actor-example",
  "nodes": [
    {
      "id": "actor",
      "type": "actor"
    }
  ]
}
```

### `rss`

**Status:** executable · free

_No schema description yet._

**Ports**

| Direction | Port | Type |
|---|---|---|
| in | — | — |
| out | `out` | source-item[] |

**Params** — passthrough (typed once the executor lands; any key is accepted).

**Example**

```json
{
  "version": "2.0",
  "name": "rss-example",
  "nodes": [
    {
      "id": "rss",
      "type": "rss"
    }
  ]
}
```

### `trend-watch`

**Status:** executable · free

_No schema description yet._

**Ports**

| Direction | Port | Type |
|---|---|---|
| in | — | — |
| out | `out` | source-item[] |

**Params** — passthrough (typed once the executor lands; any key is accepted).

**Example**

```json
{
  "version": "2.0",
  "name": "trend-watch-example",
  "nodes": [
    {
      "id": "trend-watch",
      "type": "trend-watch"
    }
  ]
}
```

### `http`

**Status:** executable · free

_No schema description yet._

**Ports**

| Direction | Port | Type |
|---|---|---|
| in | `*` (open) | any |
| out | `out` | any |

**Params** — passthrough (typed once the executor lands; any key is accepted).

**Example**

```json
{
  "version": "2.0",
  "name": "http-example",
  "nodes": [
    {
      "id": "http",
      "type": "http",
      "params": {
        "allowed_hosts": [
          "api.example.com"
        ]
      }
    }
  ]
}
```

## Related

- [Farm node-graph architecture](https://github.com/alecs5am/ralphy/blob/main/docs/architecture/farm-node-graph.md) — the design rationale behind these node types
- [`ralphy workflow`](/reference/cli/workflow) — lint and inspect a workspace's graphs
