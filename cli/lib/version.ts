/**
 * Single source of truth for the published `ralphy` version.
 *
 * Bumped by the `/release` skill in lockstep with `package.json`. Edit both
 * places when releasing manually — keep them in sync so `ralphy --version`
 * matches the npm/release tag.
 */
export const VERSION = "0.0.2";
